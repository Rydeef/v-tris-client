import styled from "styled-components";

export const ErrorMessage = styled.span`
  height: 12px;
  display: block;
  color: red;
  font-size: 14px;
  @media (max-width: 640px) {
    font-size: 12px;
  }
`;
